# ShahMaghz — آجیل و خشکبار ممتاز ایرانی

فروشگاه آنلاین لوکس آجیل، خشکبار و جعبه‌های هدیه، با تجربه‌ای شاهانه.

---

## وضعیت پروژه

| فاز | عنوان | وضعیت |
|-----|--------|--------|
| ۱ | پایه (طراحی، صفحه اصلی، صفحه محصول) | ✅ کامل |
| ۲ | تجارت (سبد خرید، تسویه حساب، حساب کاربری) | ✅ کامل |
| ۳ | محتوا و وفاداری (باشگاه، تخفیف‌ها) | ⏭️ رد شده |
| ۴ | پنل مدیریت (محصولات، سفارش‌ها، مشتریان) | ✅ کامل |
| ۵ | بهینه‌سازی و استقرار | ✅ آماده استقرار |

### موارد حذف‌شده (طبق درخواست)
- ❌ **یکپارچه‌سازی درگاه پرداخت (زرین‌پال)** — سفارش‌ها در وضعیت «در انتظار بررسی» ثبت می‌شوند و پرداخت خارج از سیستم انجام می‌شود.
- ❌ **ارسال پیامک / کد یکبار مصرف (OTP)** — ورود به صورت فرم ساده (نام، موبایل، ایمیل) انجام می‌شود.
- ❌ **فاز ۳ (باشگاه وفاداری و محتوای کامل مجله)** — صفحات موجود هستند اما منطق کامل پیاده‌سازی نشده.

---

## شروع سریع

```bash
# نصب وابستگی‌ها
npm install

# اجرای پروژه در محیط توسعه
npm run dev

# ساخت پروژه برای تولید
npm run build

# اجرای پروژه ساخته‌شده
npm start
```

پروژه در `http://localhost:3000` اجرا می‌شود.

## راه‌اندازی پایگاه داده و ذخیره‌سازی تصاویر

### 1. Neon PostgreSQL (پایگاه داده)

پروژه از Neon Tech به عنوان پایگاه داده PostgreSQL serverless استفاده می‌کند:

```bash
# 1. در neon.tech ثبت‌نام کنید و پروژه جدید بسازید
# 2. Connection strings را از داشبورد کپی کنید
# 3. در فایل .env متغیرهای زیر را تنظیم کنید:

DATABASE_URL="postgresql://user:password@ep-xxx-pooler.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require"

# 4. ساخت جداول در پایگاه داده
npx prisma db push

# 5. پر کردن پایگاه داده با داده‌های اولیه
npm run seed
```

### 2. Cloudinary (ذخیره‌سازی تصاویر)

تمام تصاویر محصولات در Cloudinary ذخیره می‌شوند:

```bash
# 1. در cloudinary.com ثبت‌نام کنید
# 2. از داشبورد، مقادیر زیر را کپی کنید
# 3. در فایل .env تنظیم کنید:

CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"
```

تصاویر محصولات از پوشه `product-images` در Cloudinary بارگذاری می‌شوند.

---

## استقرار در Vercel

### روش ۱: از طریق داشبورد Vercel

۱. به [vercel.com](https://vercel.com) بروید و وارد شوید.
۲. روی «New Project» کلیک کنید.
۳. ریپازیتوری `ShahMaghz` را از GitHub انتخاب کنید.
۴. تنظیمات به‌صورت خودکار شناسایی می‌شوند (Next.js).
۵. روی «Deploy» کلیک کنید.

### روش ۲: از طریق CLI

```bash
# نصب Vercel CLI
npm i -g vercel

# استقرار از پوشه پروژه
vercel

# برای استقرار نهایی (production)
vercel --prod
```

### متغیرهای محیط (اختیاری)

پروژه **بدون نیاز به متغیرهای محیط** کار می‌کند. برای اتصال به سرویس‌های واقعی، متغیرهای زیر را در Vercel تنظیم کنید:

```
# پایگاه داده (Neon PostgreSQL)
DATABASE_URL=postgresql://...        # اتصال Pooled
DIRECT_URL=postgresql://...          # اتصال مستقیم

# ذخیره‌سازی تصاویر (Cloudinary)
CLOUDINARY_CLOUD_NAME=...           # نام Cloud
CLOUDINARY_API_KEY=...              # کلید API
CLOUDINARY_API_SECRET=...           # Secret Key

# احراز هویت
NEXTAUTH_SECRET=...                  # کلید امنیتی NextAuth
NEXTAUTH_URL=https://your-domain.vercel.app

# پرداخت و پیامک (اختیاری)
ZARINPAL_MERCHANT_ID=...             # درگاه پرداخت (در صورت فعال‌سازی)
KAVENEGAR_API_KEY=...                # ارسال پیامک (در صورت فعال‌سازی)
```

فایل `vercel.json` شامل تنظیمات امنیتی (Security Headers) و منطقه استقرار (Singapore) است.

---

## API Routes

پروژه از Next.js API Routes برای ارتباط با پایگاه داده Neon استفاده می‌کند:

### محصولات (Products)
- `GET /api/products` - دریافت لیست محصولات (پارامترها: `categoryId`, `featured`)
- `POST /api/products` - ایجاد محصول جدید
- `GET /api/products/[id]` - دریافت جزئیات یک محصول
- `PUT /api/products/[id]` - ویرایش محصول
- `DELETE /api/products/[id]` - حذف محصول

### دسته‌بندی‌ها (Categories)
- `GET /api/categories` - دریافت لیست دسته‌بندی‌ها با تعداد محصولات

### سفارش‌ها (Orders)
- `GET /api/orders` - دریافت لیست سفارش‌ها (پارامترها: `userId`, `status`)
- `POST /api/orders` - ایجاد سفارش جدید
- `GET /api/orders/[id]` - دریافت جزئیات یک سفارش
- `PUT /api/orders/[id]` - ویرایش وضعیت سفارش

**نکته:** تمام API routes از Prisma ORM برای تعامل با Neon PostgreSQL استفاده می‌کنند و تصاویر از Cloudinary بارگذاری می‌شوند.

---

## پنل مدیریت

| مورد | مقدار |
|------|--------|
| آدرس | `/admin` |
| نام کاربری | `admin` |
| رمز عبور | `shahmaghz1403` |

> ⚠️ **هشدار:** این اطلاعات برای محیط توسعه است. در محیط تولید، حتماً با احراز هویت واقعی (NextAuth.js + نقش مدیر) جایگزین کنید.

### امکانات پنل مدیریت
- **داشبورد:** نمای کلی درآمد، سفارش‌ها، محصولات، هشدارهای موجودی کم
- **محصولات:** لیست، جستجو، ویرایش، حذف، مدیریت موجودی هر بسته‌بندی
- **سفارش‌ها:** لیست با فیلتر وضعیت، جستجو، صفحه جزئیات با تغییر وضعیت
- **مشتریان:** لیست مشتریان با آمار سفارش و خرید
- **کدهای تخفیف:** ایجاد، ویرایش، فعال/غیرفعال کردن، حذف
- **مجله:** مدیریت مقالات (منتشر شده / پیش‌نویس)
- **تنظیمات:** اطلاعات فروشگاه، تنظیمات ارسال، وضعیت درگاه و پیامک

---

## امکانات فروشگاه

### صفحه اصلی
- هیرو نامتقارن با طرح editorial (نه مرکزچین کلیشه‌ای)
- نمایش دسته‌بندی‌ها با چیدمان غیریکنواخت
- پرفروش‌ترین‌ها با کارت‌های محصول و افکت hover
- داستان برند با نقل قول و چیدمان مجله‌ای
- نظرات مشتریان با چیدمان غیریکنواخت
- فراخوان اقدام (CTA) باشگاه مشتریان

### صفحه محصول
- گالری تصاویر (placeholder — TODO: تصاویر واقعی)
- انتخاب بسته‌بندی (وزن/قیمت)
- اطلاعات تغذیه‌ای، مواد تشکیل‌دهنده، راهنمای نگه‌داری
- تب نظرات با امتیاز و نمایش نمونه
- Schema.org Product markup برای SEO
- نشان مهر مومی برای محصولات ممتاز

### دسته‌بندی
- فیلتر بر اساس موجودی و بازه قیمت
- مرتب‌سازی (قیمت، محبوبیت)
- چیدمان واکنش‌گرا (موبایل‌اول)

### سبد خرید و تسویه حساب
- سبد خرید کشویی با ذخیره‌سازی در localStorage
- فرآیند تسویه ۳ مرحله‌ای: اطلاعات ارسال → بررسی → تأیید
- انتخاب از آدرس‌های ذخیره‌شده یا ورود آدرس جدید
- محاسبه خودکار هزینه ارسال بر اساس استان
- نوار پیشرفت ارسال رایگان
- یادداشت سفارش اختیاری
- صفحه تأیید سفارش با شماره پیگیری

### حساب کاربری
- ورود/ثبت‌نام با فرم ساده (نام، موبایل، ایمیل)
- پروفایل با امکان ویرایش
- تاریخچه سفارش‌ها با وضعیت و جزئیات کامل
- مدیریت آدرس‌ها (افزودن، ویرایش، حذف، پیش‌فرض)
- لغو سفارش (فقط در وضعیت در انتظار/در حال پردازش)

### صفحات محتوایی
- مجله شاه‌مغز (لیست مقالات)
- درباره ما (داستان برند)
- تماس با ما (فرم تماس)
- سوالات متداول (FAQ آکاردئونی)
- باشگاه مشتریان (نمایش طبقات و مزایا)

---

## تکنولوژی‌ها

| دسته | تکنولوژی |
|------|----------|
| فرانت‌اند | Next.js 14 (App Router) + TypeScript + Tailwind CSS |
| مدیریت وضعیت | Zustand + persist middleware (localStorage) |
| اعتبارسنجی | Zod (با پیام‌های فارسی) |
| آیکون‌ها | Lucide React |
| فونت | Vazirmatn (Google Fonts) |
| پایگاه داده | PostgreSQL + Prisma ORM (اسکیمای موجود، اتصال اختیاری) |
| استقرار | Vercel |

---

## ساختار پروژه

```
ShahMaghz/
├── app/                            # صفحات (App Router)
│   ├── layout.tsx                  # لایه‌بندی ریشه (RTL, فونت فارسی)
│   ├── page.tsx                    # صفحه اصلی
│   ├── product/[slug]/             # صفحه جزئیات محصول
│   ├── category/[slug]/            # صفحه دسته‌بندی با فیلتر
│   ├── blog/                       # مجله
│   ├── about/                      # درباره ما
│   ├── contact/                    # تماس
│   ├── faq/                        # سوالات متداول
│   ├── loyalty/                    # باشگاه مشتریان
│   ├── account/                    # حساب کاربری
│   │   ├── page.tsx                # پروفایل + ویرایش
│   │   ├── orders/                 # سفارش‌های من + جزئیات
│   │   └── addresses/              # آدرس‌های من (CRUD)
│   ├── checkout/                   # تسویه حساب چندمرحله‌ای
│   ├── order-success/              # تأیید سفارش
│   └── admin/                      # پنل مدیریت
│       ├── layout.tsx              # لایه‌بندی با auth guard
│       ├── page.tsx                # داشبورد
│       ├── login/                  # ورود مدیر
│       ├── products/               # مدیریت محصولات + ویرایش
│       ├── orders/                 # مدیریت سفارش‌ها + جزئیات
│       ├── customers/              # مدیریت مشتریان
│       ├── discounts/              # کدهای تخفیف (CRUD)
│       ├── blog/                   # مدیریت مجله
│       └── settings/               # تنظیمات فروشگاه
├── components/
│   ├── ui/                         # Button, Card, Badge, Input, Modal, BrandMotifs
│   ├── layout/                     # Header, Footer, CartDrawer, ScrollReveal
│   ├── home/                       # Hero, CategoryShowcase, Bestsellers, BrandStory, Testimonials, CTA
│   ├── product/                    # ProductDetailClient, ProductGrid
│   ├── account/                    # AccountSidebar, LoginForm, AddressForm, AddressList, OrderList, OrderDetail
│   ├── checkout/                   # CheckoutFlow (۳ مرحله)
│   └── admin/                      # AdminSidebar, AdminLogin, AdminGuard, StatCard
├── lib/
│   ├── prisma.ts                   # کلاینت Prisma
│   ├── format.ts                   # ابزارهای فرمت فارسی (اعداد، قیمت، تاریخ jalali)
│   ├── utils.ts                    # cn() — ادغام کلاس‌های Tailwind
│   ├── cart-store.ts               # استور سبد خرید (Zustand + persist)
│   ├── auth-store.ts               # استور احراز هویت مشتری
│   ├── admin-store.ts              # استور احراز هویت مدیر
│   ├── address-store.ts            # استور آدرس‌ها + لیست استان‌ها
│   ├── order-store.ts              # استور سفارش‌ها (ایجاد، پیگیری، لغو)
│   ├── product-store.ts            # استور محصولات (CRUD + موجودی)
│   ├── discount-store.ts           # استور کدهای تخفیف (CRUD)
│   ├── validation.ts               # اعتبارسنجی Zod با پیام فارسی
│   ├── shipping.ts                 # محاسبه هزینه ارسال بر اساس استان
│   └── mock-data.ts                # داده‌های نمونه محصولات و دسته‌بندی‌ها
├── prisma/
│   └── schema.prisma               # اسکیمای کامل پایگاه داده
├── public/
│   └── images/                     # تصاویر محصول (TODO: جایگزینی با تصاویر واقعی)
├── vercel.json                     # پیکربندی استقرار Vercel
├── tailwind.config.ts              # پیکربندی Tailwind با رنگ‌های برند
├── next.config.mjs                 # پیکربندی Next.js
├── .env.example                    # نمونه متغیرهای محیط
└── ASSUMPTIONS.md                  # فرضیات و تصمیمات پروژه
```

---

## طراحی و برند

### پالت رنگ
| رنگ | کد | کاربرد |
|------|-----|--------|
| سبز جنگلی تیره | `#0F1D14` | پس‌زمینه اصلی |
| سبز جنگلی | `#1C2B1E` | پس‌زمینه بخش‌ها |
| طلایی | `#C9A24B` | رنگ تأکیدی (دکمه‌ها، لینک‌ها) |
| طلایی روشن | `#E7C873` | هاور و تأکید |
| عاج گرم | `#F5EFE0` | متن اصلی |
| شرابی | `#6E1423` | خطا و حذف |

### عناصر برند دست‌ساز
- **مهر مومی:** نشان طلایی با SVG برای محصولات ممتاز
- **خط جداکننده دست‌تراش:** استفاده کم و هدفمند
- **بافت کاغذ کرافت و کتان:** بافت‌های SVG نازک روی پس‌زمینه بخش‌ها
- **چیدمان نامتقارن:** بخش‌های editorial با چیدمان غیریکنواخت

### زبان و RTL
- `dir="rtl"` در سطح سند
- استفاده از خصوصیات منطقی Tailwind (`ms-`, `me-`, `ps-`, `pe-`)
- ارقام فارسی برای قیمت‌ها و وزن‌ها (متمرکز در `lib/format.ts`)
- فونت Vazirmatn با `font-display: swap`
- تمام متن‌های رابط کاربری به زبان فارسی

---

## مسیرهای آینده

برای تکمیل پروژه به محیط تولید واقعی:

۱. **اتصال پایگاه داده:** تنظیم `DATABASE_URL` و اجرای `npx prisma db push`
۲. **احراز هویت واقعی:** جایگزینی auth-store با NextAuth.js + OTP
۳. **درگاه پرداخت:** اتصال زرین‌پال (ساختار Order آماده است)
۴. **ارسال پیامک:** اتصال کاوه‌نگار برای OTP و اعلان‌های سفارش
۵. **تصاویر محصول:** جایگزینی placeholderها با عکس‌های واقعی
۶. **باشگاه وفاداری:** پیاده‌سازی سیستم امتیاز و طبقات
۷. **SEO:** بهینه‌سازی metadata، sitemap.xml، robots.txt

---

## فرضیات

تمام فرضیات و تصمیمات محصول در [`ASSUMPTIONS.md`](./ASSUMPTIONS.md) ثبت شده است.

---

## مجوز

این پروژه متعلق به شاه‌مغز است. © ۱۴۰۳
